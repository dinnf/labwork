let accumulator = new Accumulator(1); // initial value 1
let value1 = prompt(accumulator.read()); // adds the user-entered value
let value2 = prompt(accumulator.read()); // adds the user-entered value
console.log(accumulator.value); // shows the sum of these values

